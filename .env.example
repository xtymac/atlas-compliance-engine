# OpenAI API Key (required for AI-powered schema generation)
OPENAI_API_KEY=sk-your-api-key-here

# AI Provider: gpt-4o-mini (default) | gpt-4o-strict | gemini
SCHEMA_AI_PROVIDER=gpt-4o-mini

# Enable escalation to GPT-4o strict mode on failures (true | false)
SCHEMA_AI_STRICT_ESCALATION=true

# Server port (default: 4000)
PORT=4000
